aws_region          = "us-east-1"
project             = "image-processor"
artificial_delay_ms = 5000

# Amplify is now created via the AWS console (more reliable than the
# Terraform aws_amplify_app resource for Gen 2 SSR apps). Leave these empty.
amplify_repository  = ""
amplify_branch      = "main"
amplify_oauth_token = ""
