# Copy to .env.local and fill in after running `terraform apply`
AWS_REGION=us-east-1
S3_BUCKET=
SQS_QUEUE_URL=
DYNAMODB_TABLE=image-processor-jobs

# Local dev only: leave blank in Amplify (uses IAM role automatically)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
